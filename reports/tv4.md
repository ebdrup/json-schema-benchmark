# [`tv4`](https://github.com/geraintluff/tv4) - test summary

# All validators fail these tests

some languages do not distinguish between different types of numeric value, a float is not an integer even without fractional part


# [`tv4`](https://github.com/geraintluff/tv4) failed tests

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalItems as schema, additional items match schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalItems as schema, additional items do not match schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `items is schema, no additionalItems, all items match schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array of items with no additionalItems, no additional items present`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array of items with no additionalItems, additional items are not permitted`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalItems as false without items, items defaults to empty schema so everything is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalItems as false without items, ignores non-arrays`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalItems are allowed by default, only the first item is validated`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties being false does not allow other properties, no additional properties is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties being false does not allow other properties, an additional property is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties being false does not allow other properties, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties being false does not allow other properties, patternProperties are not additional properties`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties allows a schema which should validate, no additional properties is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties allows a schema which should validate, an additional valid property is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties allows a schema which should validate, an additional invalid property is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties can exist by itself, an additional valid property is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties can exist by itself, an additional invalid property is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `additionalProperties are allowed by default, additional properties are allowed`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf, allOf`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf, mismatch second`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf, mismatch first`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf, wrong type`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf with base schema, valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf with base schema, mismatch base schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf with base schema, mismatch first allOf`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf with base schema, mismatch second allOf`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf with base schema, mismatch both`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf simple types, valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `allOf simple types, mismatch one`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf, first anyOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf, second anyOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf, both anyOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf, neither anyOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf with base schema, mismatch base schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf with base schema, one anyOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `anyOf with base schema, both anyOf invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `invalid type for default, valid when property is specified`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `invalid type for default, still valid when the invalid default is used`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `invalid string value for default, valid when property is specified`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `invalid string value for default, still valid when the invalid default is used`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `valid definition, valid definition schema`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `invalid definition, invalid definition schema`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `dependencies, neither`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `dependencies, nondependant`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `dependencies, with dependency`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `dependencies, missing dependency`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `dependencies, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, neither`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, nondependants`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, with dependencies`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, missing dependency`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, missing other dependency`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies, missing both dependencies`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies subschema, valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies subschema, no dependency`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies subschema, wrong type`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies subschema, wrong type other`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple dependencies subschema, wrong type both`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `simple enum validation, one of the enum is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `simple enum validation, something else is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `heterogeneous enum validation, one of the enum is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `heterogeneous enum validation, something else is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `heterogeneous enum validation, objects are deep compared`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `enums in properties, both properties are valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `enums in properties, missing optional property is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `enums in properties, missing required property is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `enums in properties, missing all properties is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `a schema given for items, valid items`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `a schema given for items, wrong type of items`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `a schema given for items, ignores non-arrays`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `an array of schemas for items, correct types`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `an array of schemas for items, wrong types`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxItems validation, shorter is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxItems validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxItems validation, too long is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxItems validation, ignores non-arrays`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxLength validation, shorter is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxLength validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxLength validation, too long is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxLength validation, ignores non-strings`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxLength validation, two supplementary Unicode code points is long enough`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxProperties validation, shorter is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxProperties validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxProperties validation, too long is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maxProperties validation, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maximum validation, below the maximum is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maximum validation, above the maximum is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `maximum validation, ignores non-numbers`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `exclusiveMaximum validation, below the maximum is still valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `exclusiveMaximum validation, boundary point is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minItems validation, longer is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minItems validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minItems validation, too short is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minItems validation, ignores non-arrays`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minLength validation, longer is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minLength validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minLength validation, too short is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minLength validation, ignores non-strings`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minLength validation, one supplementary Unicode code point is not long enough`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minProperties validation, longer is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minProperties validation, exact length is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minProperties validation, too short is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minProperties validation, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minimum validation, above the minimum is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minimum validation, below the minimum is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `minimum validation, ignores non-numbers`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `exclusiveMinimum validation, above the minimum is still valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `exclusiveMinimum validation, boundary point is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by int, int by int`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by int, int by int fail`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by int, ignores non-numbers`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by number, zero is multiple of anything`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by number, 4.5 is multiple of 1.5`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by number, 35 is not multiple of 1.5`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by small number, 0.0075 is multiple of 0.0001`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `by small number, 0.00751 is not multiple of 0.0001`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not, allowed`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not, disallowed`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not multiple types, valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not multiple types, mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not multiple types, other mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not more complex schema, match`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not more complex schema, other match`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `not more complex schema, mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `forbidden property, property present`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `forbidden property, property absent`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf, first oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf, second oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf, both oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf, neither oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf with base schema, mismatch base schema`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf with base schema, one oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `oneOf with base schema, both oneOf valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer, a bignum is an integer`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number, a bignum is a number`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer, a negative bignum is an integer`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number, a negative bignum is a number`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string, a bignum is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer comparison, comparison works for high numbers`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `float comparison with high precision, comparison works for high numbers`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer comparison, comparison works for very negative numbers`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `float comparison with high precision on negative numbers, comparison works for very negative numbers`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of date-time strings, a valid date-time string`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of date-time strings, an invalid date-time string`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of date-time strings, only RFC3339 not all of ISO 8601 are valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of URIs, a valid URI`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of URIs, an invalid URI`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of URIs, an invalid URI though valid URI reference`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of e-mail addresses, a valid e-mail address`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of e-mail addresses, an invalid e-mail address`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IP addresses, a valid IP address`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IP addresses, an IP address with too many components`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IP addresses, an IP address with out-of-range values`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IP addresses, an IP address without 4 components`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IP addresses, an IP address as an integer`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IPv6 addresses, a valid IPv6 address`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IPv6 addresses, an IPv6 address with out-of-range values`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IPv6 addresses, an IPv6 address with too many components`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of IPv6 addresses, an IPv6 address containing illegal characters`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of host names, a valid host name`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of host names, a host name starting with an illegal character`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of host names, a host name containing illegal characters`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `validation of host names, a host name with a component too long`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `pattern validation, a matching pattern is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `pattern validation, a non-matching pattern is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `pattern validation, ignores non-strings`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `patternProperties validates properties matching a regex, a single valid match is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `patternProperties validates properties matching a regex, multiple valid matches is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `patternProperties validates properties matching a regex, a single invalid match is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `patternProperties validates properties matching a regex, multiple invalid matches is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `patternProperties validates properties matching a regex, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, a single valid match is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, a simultaneous match is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, multiple matches is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, an invalid due to one is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, an invalid due to the other is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple simultaneous patternProperties are validated, an invalid due to both is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `regexes are not anchored by default and are case sensitive, non recognized members are ignored`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `regexes are not anchored by default and are case sensitive, recognized members are accounted for`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `regexes are not anchored by default and are case sensitive, regexes are case sensitive`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `regexes are not anchored by default and are case sensitive, regexes are case sensitive, 2`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object properties validation, both properties present and valid is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object properties validation, one property invalid is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object properties validation, both properties invalid is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object properties validation, doesn't invalidate other properties`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object properties validation, ignores non-objects`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, property validates property`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, property invalidates property`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, patternProperty invalidates property`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, patternProperty validates nonproperty`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, patternProperty invalidates nonproperty`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, additionalProperty ignores property`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, additionalProperty validates others`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `properties, patternProperties, additionalProperties interaction, additionalProperty invalidates others`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `root pointer ref, match`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `root pointer ref, recursive match`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `root pointer ref, mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `root pointer ref, recursive mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `relative pointer ref to object, match`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `relative pointer ref to object, mismatch`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `relative pointer ref to array, match array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `relative pointer ref to array, mismatch array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `escaped pointer ref, slash`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `escaped pointer ref, tilda`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `escaped pointer ref, percent`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `nested refs, nested ref valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `nested refs, nested ref invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `remote ref, containing refs itself, remote ref valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `remote ref, containing refs itself, remote ref invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `remote ref, remote ref valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `remote ref, remote ref invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `fragment within remote ref, remote fragment valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `fragment within remote ref, remote fragment invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `ref within remote ref, ref within ref valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `ref within remote ref, ref within ref invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `change resolution scope, changed scope ref valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `change resolution scope, changed scope ref invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `required validation, present required property is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `required validation, non-present required property is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `required default validation, not required by default`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, an integer is an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, a float is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, a string is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, an object is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, an array is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, a boolean is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `integer type matches integers, null is not an integer`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, an integer is a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, a float is a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, a string is not a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, an object is not a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, an array is not a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, a boolean is not a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `number type matches numbers, null is not a number`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, 1 is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, a float is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, a string is a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, an object is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, an array is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, a boolean is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `string type matches strings, null is not a string`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, an integer is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, a float is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, a string is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, an object is an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, an array is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, a boolean is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `object type matches objects, null is not an object`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, an integer is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, a float is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, a string is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, an object is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, an array is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, a boolean is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `array type matches arrays, null is not an array`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, an integer is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, a float is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, a string is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, an object is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, an array is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, a boolean is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `boolean type matches booleans, null is not a boolean`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, an integer is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, a float is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, a string is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, an object is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, an array is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, a boolean is not null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `null type matches only the null object, null is null`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, an integer is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, a string is valid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, a float is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, an object is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, an array is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, a boolean is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `multiple types can be specified in an array, null is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`). **This excludes this validator from performance tests**

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, unique array of integers is valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, non-unique array of integers is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, numbers are unique if mathematically unequal`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, unique array of objects is valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, non-unique array of objects is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, unique array of nested objects is valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, non-unique array of nested objects is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, unique array of arrays is valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, non-unique array of arrays is invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, 1 and true are unique`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, 0 and false are unique`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, unique heterogeneous types are valid`. Because the schema failed to load(`Maximum call stack size exceeded`)

[`tv4`](https://github.com/geraintluff/tv4) failed the test `uniqueItems validation, non-unique heterogeneous types are invalid`. Because the schema failed to load(`Maximum call stack size exceeded`)

**All other tests passed**.
